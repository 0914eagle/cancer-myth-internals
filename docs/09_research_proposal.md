# 09. 연구계획 — 주장, 가설, Task, 표, 예상 contribution

2026-09-08 작성, 2026-09-09 갱신. **최종 결과 보고서가 아니라 가설과 검증 절차를 명세한 연구계획**이다. E1 탐색 결과를 본 뒤 정교화한 부분이 있으므로 모든 선택이 데이터 관찰 전에 사전 등록됐다고 쓰지 않는다. 기존 03의 원인 가설 H1–H6, 06의 실험 번호, 서버 E1/E2 이름과 아래 RH1–RH3 및 Task 1–3을 구분한다. 현재 주장·가설은 이 문서, 실행 규칙은 [13](13_task_spec.md), Introduction부터 Conclusion까지의 원고 구성은 [17](17_manuscript_storyline.md)을 따른다. 구현 완료나 실험 성공을 뜻하지 않는다.

## 1. 교수님께 설명할 한 문장

> 의료 질문의 교정 필요성을 판별해 선택적으로 개입함으로써, 정상 질문과 일반 의료 QA의 성능 손실을 제한하면서 거짓 전제 교정 성능을 개선할 수 있다는 가설을 검증한다. 내부 신호가 필요한지, 활성값 개입이 프롬프트 개입보다 유리한지는 각각 입증할 하위 가설이다.

결과가 지지할 때의 논문 주장: **내부 신호로 교정 대상을 선택하면, 정상 질문의 성능 손실을 사전 허용폭 내로 제한하면서 의료 거짓 전제 교정 성능을 높일 수 있다.**

“실험을 해보고 연구 주제를 정하겠다”가 아니다. 문제 → 주장 → 반증 가능한 가설 → 검증 실험 → 예상 기여의 순서다. 실험은 가설을 지지하도록 설계하는 것이 아니라, 지지와 반박을 구분하도록 설계한다. 기각되면 최종 주장의 범위를 조정한다.

## 2. 서사

1. 환자 질문에는 잘못된 의학적 믿음이 전제로 들어갈 수 있다. 표면 질문에 대한 사실적으로 맞는 답만으로 그 오해가 해소되지는 않는다.
2. 전제 검사를 강화하는 기존 완화책은 교정률을 높이지만, 시험된 설정에서 정상 전제를 잘못 부정하거나 다른 의료 QA 점수를 떨어뜨리는 성능 상충을 보였다. 구체적인 근거와 한계는 [10](10_evidence_and_baselines.md)에 정리했다.
3. 따라서 목표는 교정을 무조건 강화하는 것이 아니라 **교정할 질문을 정확히 선택하는 것**이다. 이것이 선택적 개입의 동기다.
4. 그 선택에 질문·CoT 기반 판단 외에 내부 표상이 추가 정보를 줄 수 있다는 가설을 세운다. “CoT가 불가능하므로 내부만 가능하다”는 논리는 사용하지 않는다.
5. 기존 조건부 개입을 출발점으로 삼아, 의료 전제 교정에서 내부 신호의 가치와 개입 방식의 효과를 분리하고 정상 질문 보존까지 평가한다.

## 3. 가설과 판정 기준

| 가설 | 사전 예측 | 반박되거나 제한되는 경우 | 검증 |
|---|---|---|---|
| **RH1. 내부 신호의 추가 가치** | 같은 held-out 질문에서 내부 readout이 질문·CoT 기반 readout보다 교정 필요성을 잘 판별하거나, 출력 신호에 추가했을 때 예측이 개선된다 | 출력과 동등하거나 열세이고 결합해도 개선이 없다. 보편적인 내부 우월성은 주장하지 않는다 | Task 1 |
| **RH2. 선택적 개입의 효과** | 같은 교정 연산에서 내부 신호로 대상을 고르면, 항상 개입 및 출력 기반 선택보다 정상 질문 손실 제약 아래 FPQ 성능이 좋아진다 | 동일한 제약에서 개선이 없거나 다른 게이트가 더 낫다 | Task 2 |
| **RH3. 일반 의료 QA 보존** | Task 2에서 고정한 시스템이 별도 의료 QA에서 Plain 대비 성능을 허용폭 내로 유지한다 | 성능 차이의 신뢰구간이 비열등성을 뒷받침하지 못한다. 단순히 유의한 하락이 없다는 것으로 통과시키지 않는다 | Task 3 |

추가 가설 RH2b: **동일한 게이트 뒤에서** 활성값 개입이 프롬프트 개입보다 교정 품질을 높일 수 있다. 이는 steering을 주력 방법으로 주장할 때 필요한 비교이며 RH1/RH2와 별개다. 프롬프트가 이기면 그 결과를 보고한다.

## 4. 방법론의 최소 명세

**구체적 주 실행안:** [19 — Method 프로토콜](19_method_protocol.md). 원 질문 마지막 프롬프트 토큰의 표준화+L2 logistic으로 A를 학습하고, 같은 fit FPQ의 교정/비교정 참조 응답 첫 32토큰 평균 차로 C를 만든다. 별도 개입 없는 pass에서 gate를 읽은 뒤 선택된 질문의 마지막 프롬프트 위치와 이후 decode에 고정 방향을 더한다. probe·C의 데이터와 역할은 다르며, 아래 확장 후보보다 19의 주 명세가 우선한다.

입력은 환자 질문 q, 타깃 모델은 M이다. 배포 시 정답 전제 라벨은 제공하지 않는다.

1. 정해진 읽기 시점에서 교정 필요성 점수 s(q)를 얻는다.
2. 개발 데이터로 정한 문턱 τ를 넘으면 교정 연산 I를 적용한다.
3. 그렇지 않으면 기준 답변 정책을 따른다.

```text
환자 질문 → 교정 필요성 readout → gate
                               ├─ 열림 → 교정 프롬프트 또는 activation steering
                               └─ 닫힘 → 기준 답변
```

- **게이트**는 누구에게 개입할지 정한다. 질문 분류기, CoT monitor, linear probe, AO/NLA 기반 점수가 후보다.
- **개입**은 선택된 질문에 무엇을 할지 정한다. 교정 프롬프트, residual 방향, head별 방향이 후보다.
- 같은 질문도 게이트가 틀릴 수 있고, 올바르게 선택해도 교정이 틀릴 수 있다. 탐지와 교정 성공을 별도로 평가한다.
- CAST와 probe 기반 라우팅의 구조를 재사용할 수 있다. 최초의 조건/행동 분리나 최초의 의료 gated steering이라고 주장하지 않는다. [11의 직접 선행연구 검토](11_tripathi_review.md)를 반드시 함께 읽는다.

개발 단계 선택 목표의 개념식:

```text
maximize  FPQ correction
subject to  NFP score loss ≤ ε_NFP
            TPQ false-correction increase ≤ ε_TPQ
```

별도 의료 QA는 고정된 시스템의 전이 평가다. 의료 QA로도 정책을 조정한다면, QA 개발셋과 최종 test를 별도로 마련하고 조정 사실을 공개한다. 위 제약식 자체를 새 알고리즘으로 주장하지 않는다. ε는 결과를 본 뒤 통과하도록 고르지 않는다.

## 5. Task 1 — 교정 필요성 탐지와 내부 신호

**질문:** 내부에서 읽는 것이 질문이나 CoT를 읽는 것보다 유용한가?

라벨은 하나다: **질문에 거짓 전제가 있는가** (전문가 검증 FPQ = 1, TPQ/NFP = 0). “Plain이 교정을 실패할 것인가”는 다른 예측 목표이므로 Table 1에 섞지 않고 부록 소표와 Task 2의 A∧C 조건에서만 다룬다.

비교 readout:

- 질문만 보고 직접 전제 검사, extract-and-verify, 학습된 텍스트 readout.
- 질문과 생성된 CoT를 읽는 monitor. 명시적 전제 검사 CoT도 포함한다.
- 타깃 모델 prefill의 linear probe와 difference-of-means.
- AO/NLA 기반 점수는 지원 체크포인트와 사전 정의한 판정 절차가 마련되면 비교한다.
- 텍스트+내부 결합 readout으로 추가 정보 여부를 검사한다. BoW는 단순 표면 기준선이지 모든 텍스트 방법의 수학적 상한이 아니다.

**입력·시점 공정성:** prefill gate와 CoT 이후 gate는 정보와 비용이 다르다. 조기 판단 비교와 CoT 생성 이후의 같은 시점 비교를 구분한다. 최종 정답·judge 라벨이 monitor 입력에 들어가면 안 된다. 학습량·개발 탐색 예산·모델 크기·추론 토큰·시간을 기록한다.

지표: AUROC, 낮은 FPR 운영점의 TPR, calibration 및 추론 비용. 개발셋에서 문턱을 고정하고 test에서 실제 FPR과 TPR을 보고한다. 층 선택도 개발 데이터 안에서 한다.

## 6. Task 2 — FPQ 교정과 TPQ/NFP 보존

**질문:** 선택적 교정이 기존 완화책의 성능 상충을 개선하는가?

Cancer-Myth 및 Well-Actually 평가를 기반으로 한다. 핵심 비교 행은 Plain, 명시적 전제 검사 CoT, FP Identification, Extract+FactCheck, GEPA, 기존 조건부 방법, 평가 대상 정책이다. 공개 모델에 원래 없는 baseline 행은 직접 실행한 확장으로 표시한다.

Table 2는 완성된 방법을 공통 개발·보존 규칙 아래 각각 조정해 비교한다. Table 3은 부품 하나를 바꾸는 통제 비교다. 최종 방법 비교와 부품별 효과를 구분한다.

Table 3은 논문용 두 패널로 구성한다. 각 행에 PCR·PCS·NFP·TPQ 오교정률의 절대값과 95% CI 및 개입률을 넣는다. 실제 행·열·캡션은 [13 §2](13_task_spec.md)와 [17 §6](17_manuscript_storyline.md)에 있다.

| 패널 | 고정하는 것 | 비교할 행 | 무엇을 판별하는가 |
|---|---|---|---|
| **3(a) 선택 방식** | 동일 C 방향·강도·위치·구간; 일부 선택 조건은 동일 K | 전체 / 무작위 K / 텍스트 상위 K / CoT 상위 K / 내부 상위 K | 개입량 감소와 선택 정보의 효과; 어떤 신호가 실제 교정에 유용한 질문을 고르는가 |
| **3(b) 개입 방식** | 3(a)의 내부 선택 문항 목록 | 교정 프롬프트 / 같은 C steering | 동일 대상에서 교정 연산의 효과 |

무작위 gate는 같은 선택 예산과 여러 seed를 쓴다. Table 3의 상위 K 예산 통제는 test 라벨을 쓰지 않는 배치 진단이며, Table 2의 고정 문턱 정책과 구분한다 ([13 §2](13_task_spec.md)). 항상 개입은 비율을 맞출 수 없으므로 강도를 맞춘 비교와 성능 곡선 비교를 구분한다. 프롬프트를 바꾼 뒤 gate를 다시 계산하면 동일 gate 비교가 아니다. A∧C와 head steering은 추가 진단으로 둔다.

oracle gate는 gold FPQ 라벨로 개입하는 진단 조건이다. gold가 있어도 모든 FPQ에 개입하는 것이 항상 최적은 아니므로 전체 성능의 엄밀한 상한이라고 부르지 않는다.

지표: PCR, PCS, 공식 NFP, Well TPQ 점수와 오교정률. TPQ 답변 내용의 정확성은 전제 부정 여부와 별도로 측정한다. 전체 평균과 문항별 harm/rescue, 게이트 발화율, 비용을 함께 보고한다.

## 7. Task 3 — 일반 의료 QA 성능 보존

**질문:** FPQ 전용 정책이 다른 의료 질문을 받았을 때도 성능을 유지하는가?

MedQA, PubMedQA, Medbullets를 우선 계획으로 둔다. 이는 기존 Cancer-Myth의 평가 축을 계승한 보존 실험이며 AO/NLA의 별도 toy task가 아니다. 세 벤치마크만 실행하면 원본의 모든 QA 열을 재현했다고 부르지 않는다.

Task 2의 개발 절차를 거쳐 고정한 **전체 시스템**을 적용한다. cross-fitting의 fold별 모델과 별도로 QA용 최종 train/calibration 및 선택 규칙을 사전에 지정한다. 새 probe의 문턱을 기존 fold 문턱의 평균으로 대체하지 않는다 ([13 §3](13_task_spec.md)). 평가셋 이름을 보고 gate를 강제로 끄지 않는다. 각 벤치마크의 원래 정확도·채점 규칙과 gate 발화율, Plain 대비 paired 변화 및 신뢰구간을 보고한다. Task 3은 제안 시스템의 보존 평가이며 독립적인 새 방법 기여가 아니다.

## 8. 논문 표 — Task 번호와 표 번호를 혼동하지 않는다

실행 수준의 행·열·출처는 [13](13_task_spec.md). 요약:

| 산출물 | 열 | 행 / 역할 | 연결 |
|---|---|---|---|
| **Table 1: 판별 신호 비교** | AUROC와 CI, dev에서 고정한 문턱의 test TPR·실제 FPR | 텍스트·직접 질문·extract-and-verify·CoT monitor·hidden·결합 | **Task 1**. 무엇으로 거짓 전제 문항을 구분할 수 있는가 |
| **Table 2: 본 결과** | PCR, PCS, NFP, TPQ 오교정, MedQA, PubMedQA, Medbullets와 CI | Plain / FP Identification / GEPA / 균형 전제 검토 CoT / Unconditional C / CAST / Hidden + FP prompt / Hidden + C. 출판 수치는 참고 행으로 분리 | **Task 2 + Task 3**. 완성된 방법의 교정·보존 효용; 원 평가 요소를 계승한 재실행 비교 |
| **Table 3: 구성 요소 분석** | PCR·PCS·NFP·TPQ 오교정률의 절대값과 CI, 개입률; 본문에 paired 차이 | (a) 선택 방식 5행 / (b) 같은 문항 목록의 개입 방식 2행 | **Task 2**. 어떤 부품이 결과에 기여했는가. Table 2와 설정이 같으면 기준 행을 참조 |
| **Figure 1: FPQ–TPQ** | x = TPQ 보존, y = PCR | 완성된 정책만. Table 3 배치 진단을 넣으면 별도 표시 | Well Figure 1의 표현을 참고하되 원문의 평균 점수와 구분 |
| 부록 | 카테고리, A×C 분포, 층별 cosine, AO/NLA 통제, Well 판정기 재채점, 일반 도메인 3종 | 진단 및 해석 한계 | 주장을 보조 |

Task와 Table은 일대일이 아니다. 표는 **판별 정보 → 최종 효용 → 구성 요소의 효과**를 각각 보여준다. 의료 QA의 ACC 세 열은 Table 2에 통합하고, Table 3(a)/(b)는 별도의 구성 요소 결과표로 둔다. 같은 결과를 다시 배치한 것을 독립 증거로 세지 않는다. [17 §6](17_manuscript_storyline.md)에 실제 읽는 법과 원고 문단을 적었다.

평가 단위는 585 전체와 150 전체다. 학습형 부품(probe, C 방향, CAST, GEPA, 분류기)은 nested grouped 5-fold cross-fitting으로 전 문항에 out-of-fold 결과를 낸다. 그래야 Cancer-Myth Table 1·3과 분모가 같다. Well의 잠근 분할은 저자들이 원 실험에서 사용한 **분할별 문항 식별자 목록**을 확보하면 부록에서 Well 판정기로 한 번 더 한다 ([12 §5](12_discussion_decisions.md)).

원본 표의 숫자를 그대로 붙이려면 **모델 버전, 문항 ID, 분할, 입력, few-shot/RAG, 생성 및 판정 조건**이 같아야 한다. 닫힌 모델 행과 판정 시점이 다른 행은 참고 결과로 분리한다. 기존 표의 형식을 쓰는 것과 기존 숫자를 직접 비교하는 것은 다르다.

## 9. 예상 contribution — 실험이 가설을 지지할 때

- **내부 신호의 추가 가치.** 의료 질문에서 질문 텍스트와 명시적 전제 검토 CoT에 비해, 내부 표상이 낮은 정상 질문 오탐 조건에서 거짓 전제 질문을 판별하고 유용한 개입 대상을 선택하는 데 추가 가치를 제공함을 보인다.
- **교정 개입의 추가 가치.** 동일한 질문을 선택한 상태에서, 활성값 개입이 교정 프롬프트보다 정확한 전제 교정을 개선하는 조건과 정상 질문에 미치는 영향을 규명한다.
- **교정과 성능 보존의 동시 달성.** 선택과 개입을 결합한 시스템이 기존 비교 방법 대비 의료 전제 교정의 성능 상충을 개선하고, 정상 질문과 일반 의료 QA의 손실을 사전 허용폭 안으로 제한함을 보인다.

2026-09-09 발표 준비에서 이전의 ‘방법·분석·실증’ 분류를 위와 같이 구체화했다. 전체 연구의 지위는 현재 **기존 조건부 방법의 적용·분석**이며, 예상 contribution과 그 근거를 설명하는 상세 원고는 [18](18_professor_presentation_script.md)에 있다. 새로운 알고리즘 기여를 원한다면 기존 방법의 구체적 한계, 변경, 그 변경을 검증할 ablation을 추가로 명세해야 한다. AO/NLA 사용이나 허용폭 추가만으로 방법론 독창성을 확정하지 않는다. RH1이 지지되지 않으면 내부 신호의 우위 문장을, RH2b가 지지되지 않으면 steering의 우위 문장을 사용하지 않는다. 보존 제약을 입증하지 못하면 세 번째 문장을 달성한 결과처럼 쓰지 않고, 확인된 손실과 불확실성을 구분해 보고한다. 세 표를 만들었다는 사실만으로 독립적인 기여 세 개가 확보되는 것은 아니다.

## 10. 실행 전에 고정할 사항

1. 질문/동일 myth/의역/TPQ–NFP 중복 그룹과 train–dev–test ID manifest. Well의 보고된 개수만으로 저자 split을 확보했다고 가정하지 않는다.
2. 모델 체크포인트, 생성 및 monitor 읽기 시점, train 라벨과 judge 버전.
3. ε_NFP·ε_TPQ·QA별 허용폭의 의미와 값, 일차 지표, 신뢰수준 및 다중 비교 처리.
4. gate·방향·층·강도·정렬·무작위 seed 선택 절차. 최종 test는 선택에 쓰지 않는다.
5. 학습/평가 누수, judge 파싱·캐시 혼합, TPQ rubric 관련 기존 코드 리뷰의 해결 여부. 문서 작성은 코드 수정 완료를 뜻하지 않는다.

논의의 변경 내역과 아직 결정되지 않은 사항은 [12](12_discussion_decisions.md)에 남긴다.
