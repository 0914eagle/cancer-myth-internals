# 17. 논문 구성 — Introduction부터 Conclusion까지

2026-09-09. 교수님께 설명하고 논문을 쓰기 위한 **절별 작성 설계**다. 가설은 [09](09_research_proposal.md), 실행 명세는 [13](13_task_spec.md), 문헌 근거는 [10](10_evidence_and_baselines.md)·[11](11_tripathi_review.md)을 따른다. 아래 예상 결론은 실험 결과가 지지할 때만 사용할 문장이다. 표의 빈칸은 미실행·미확정 결과이며, 이전 대화의 설명용 가상 숫자는 넣지 않는다.

## 0. 논문 전체를 관통하는 주장

**연구 질문:** 의료 질문에서 교정할 대상을 선택하면, 정상 질문과 일반 의료 QA의 손실을 제한하면서 잘못된 전제를 더 잘 교정할 수 있는가? 그 선택에 내부 표상이, 선택 후 개입에 activation steering이 추가로 기여하는가?

**가설이 모두 지지될 때의 한 문장 주장:**

> 의료 질문의 내부 표상으로 교정 대상을 선택하고 교정 개입을 적용하여, 정상 질문과 일반 의료 QA의 성능을 사전 허용폭 내로 유지하면서 거짓 전제 교정 성능을 개선한다.

이 문장의 세 부분을 따로 검증한다. **내부 신호의 추가 가치(RH1)**, **선택적 교정의 효과와 개입 방식의 차이(RH2/RH2b)**, **의료 QA 보존(RH3)**이다. 활성값 개입의 우위가 확인되지 않으면 steering의 우월성을 결론에 넣지 않는다. 내부 신호의 우위가 확인되지 않으면 내부가 필요하다는 주장을 빼고 선택적 개입의 결과와 한계를 보고한다.

**서사 한 줄:** 전제 교정 누락 → 기존 완화책에서 관찰된 성능 상충 → 교정 대상을 선택하자는 가설 → 선택 신호와 교정 연산의 명세 → 최종 효용과 부품별 효과의 검증 → 성립 조건과 한계.

**현재 연구의 지위:** 조건부 개입의 의료 배경 전제 적용·분석이다. CAST의 조건/행동 분리, Two Axes의 probe 기반 프롬프트 라우팅, Tripathi의 의료 내부 gate와 steering이 선행한다. 데이터·목표·평가를 바꾼 것만으로 새로운 알고리즘이라고 부르지 않는다. 추가 알고리즘 기여를 주장하려면 구체적 변경과 그 변경만 제거한 비교가 필요하다.

## 1. 논문 목차와 각 절이 답할 질문

| 절 | 독자의 질문 | 채울 핵심 내용 | 다음 절로 연결 |
|---|---|---|---|
| **1. Introduction** | 왜 이 문제를 풀어야 하며 무엇을 주장하는가? | 전제 교정 누락, 기존 완화책의 성능 상충, 선택적 교정 가설, 예상 기여 | 어떤 선행 방법에서 출발하는가 |
| **2. Related Work** | 기존 연구가 이미 한 것과 남은 질문은 무엇인가? | 의료 FPQ 평가, prompting/CoT, 내부 readout, 조건부 steering | 우리 방법에서 실제로 선택·검증할 부품 |
| **3. Methodology** | 입력을 받아 어떤 절차로 답하며, 무엇을 학습하는가? | 문제 정의, gate, 교정 개입, 제약 아래 개발, 비교 조건 | 이 정책을 어떻게 공정하게 평가하는가 |
| **4. Experimental Setup** | 데이터·baseline·판정·분할이 타당한가? | Cancer-Myth/Well 활용 범위, 모델, nested grouped cross-fitting, 지표·허용폭 | 세 연구 질문을 검증하는 결과 |
| **5. Experimental Results** | 신호가 유용한가, 최종 성능이 좋은가, 어느 부품이 기여했는가? | Table 1 판별, Table 2 최종 효용, Table 3 통제 비교 | 성공·실패의 범위를 해석 |
| **6. Discussion and Limitations** | 결과가 어디까지 말해주며 무엇은 남았는가? | readout 해석, 작은 정상 표본, judge·도메인·방법 독창성 한계 | 검증한 사실만 결론으로 |
| **7. Conclusion** | 결국 무엇을 알게 되었는가? | 문제, 지지된 결과, 적용 범위 | 새 주장·새 결과를 추가하지 않음 |

지면이 짧으면 Experimental Setup과 Results를 Experiments의 하위 절로 합친다. Task는 평가 질문이며 독립 방법 세 개가 아니다. Task와 contribution도 일대일 대응하지 않는다.

## 2. Introduction — 다섯 문단으로 작성

### 문단 1: 답변의 사실성과 전제 교정은 다른 평가 대상이다

**할 말:** 환자는 잘못된 믿음을 질문의 전제로 포함할 수 있다. 답변이 표면 질문에만 대응하면 그 믿음을 명시적으로 바로잡지 못할 수 있다. Cancer-Myth는 이 문제를 전문가 검증 질문과 교정 지표로 평가했다.

**문장 초안:**

> 의료 질문에 유용하게 답하려면 요청된 정보뿐 아니라 질문이 전제하는 주장도 검토해야 한다. 잘못된 전제를 그대로 둔 답변은 환자의 오해를 해소하지 못할 수 있다. Cancer-Myth는 이러한 전제 교정 누락을 평가하며, 의료 QA의 정답 정확도와 별도로 측정할 필요를 보여준다.

근거: [Cancer-Myth](https://arxiv.org/html/2504.11373v3), [10 §1](10_evidence_and_baselines.md#1-cancer-myth--문제와-본-평가표의-출발점). 예시는 검증된 benchmark 문항과 반박을 사용한다. 서로 다른 시험의 백분율을 직접 빼서 능력 차이를 추정하지 않는다. 임상적 위해 감소는 이번 benchmark만으로 입증하지 않는다.

### 문단 2: 목표는 높은 FPQ 점수와 정상 질문 보존을 함께 달성하는 것이다

**할 말:** 전제 검사 지시·프롬프트 최적화 등의 방법은 평가된 설정에서 FPQ 교정을 개선했지만, 정상 전제를 부당하게 부정하거나 다른 의료 QA 점수가 하락하는 경우가 있었다.

**문장 초안:**

> 기존 완화책은 전제 교정 성능을 높일 수 있지만, 정상 질문에서도 교정을 시도하는 부작용이 보고되었다. Cancer-Myth와 Well-Actually의 결과는 교정률만으로 방법을 평가하기 어렵다는 점을 보여준다. 우리는 정상 질문의 손실을 사전에 정한 범위로 제한하면서 거짓 전제 교정을 개선하는 문제를 다룬다.

근거: Cancer-Myth Table 1, [Well-Actually](https://arxiv.org/html/2608.06539v1), [10 §1–2](10_evidence_and_baselines.md). 정상 전제 오교정과 의료 QA 하락을 구분해 인용한다. 모든 prompting의 실패나 모든 모델에서의 성능 하락으로 일반화하지 않는다. WildChat의 FPQ 비율을 의료 질문의 발생률로 가정하지 않는다.

### 문단 3: 선택적 개입이라는 가설과 선행 방법의 위치

**할 말:** 개입 대상을 고르는 부품과 실제로 고치는 부품을 나누어 평가하면, 정상 질문의 손실을 줄이는 선택 정책을 검증할 수 있다. 이 구조 자체는 기존 조건부 개입에서 가져온다.

**문장 초안:**

> 이를 위해 교정 대상을 판별하는 단계와 선택된 질문에 교정 연산을 적용하는 단계를 구성한다. 조건부 steering과 probe 기반 라우팅은 이러한 접근의 출발점이다. 의료 질문의 배경 전제와 어려운 정상 질문에서 어떤 선택 신호와 개입이 유용한지는 동일한 평가 조건에서 확인해야 한다.

근거: CAST, Two Axes, Tripathi. Related Work에서 직접 비교한다. CAST가 조건과 행동을 구분하지 못했다거나 Tripathi가 내부 신호를 쓰지 않는다고 쓰지 않는다.

### 문단 4: 왜 내부를 보나 — CoT를 포함한 비교 가설

**할 말:** 내부 readout은 생성 이전 신호를 활용할 수 있는 후보이다. CoT의 불완전한 충실성은 비교의 동기가 되지만 CoT monitor의 무용성을 뜻하지 않는다. 명시적 전제 검사 CoT와 내부 방법을 모두 평가한다.

**문장 초안:**

> 우리는 질문 텍스트와 생성된 전제 검토 설명에 더해, 타깃 모델의 내부 표상이 교정 대상 선택에 추가 정보를 제공하는지 검증한다. CoT prompting은 최종 답변을 개선하는 비교 대상으로, CoT monitoring은 개입 대상을 고르는 비교 대상으로 포함한다. 또한 동일한 선택 결과 아래 프롬프트 개입과 활성값 개입을 비교한다.

근거: [10 §5](10_evidence_and_baselines.md#5-cot로-하면-안-되는가에-대한-근거의-구분). prefill probe의 비용·지연 이점도 실제로 측정해야 한다. 성능이 같다는 이유만으로 내부 방법이 더 싸다고 단정하지 않는다. 모델의 내부 계산 전체나 숨겨진 믿음을 읽는다고 주장하지 않는다.

### 문단 5: 접근 요약, 주 결과 예고, contribution 세 개

**접근 요약:** 질문의 prefill에서 probe 점수를 계산하고, 개발 데이터로 정한 문턱을 넘는 질문에 교정 개입을 적용한다. 방향과 문턱은 학습·개발 데이터에서 정하며 최종 평가에서는 고정한다.

**결과 예고 슬롯:** "[모델·평가 범위]에서 Plain 대비 PCR이 [Δ와 CI] 변했고, NFP·TPQ 및 [QA 평가]의 손실은 [허용폭·CI]였다." 아직 이 슬롯에 탐색 결과나 가상 숫자를 넣지 않는다. Table 2의 주 결과를 여기서 먼저 소개한다.

**가설이 지지될 때 사용할 contribution 초안:**

1. **방법·정책:** 의료 배경 전제 교정을 위해 정상 질문 보존을 명시한 선택적 교정 정책을 구성하고, 기존 조건부 개입 방법과 비교한다.
2. **분석:** 텍스트·CoT 대비 내부 신호의 판별 및 선택 가치와, 동일한 선택 아래 활성값 개입의 추가 효과를 분리해 정량화한다.
3. **실증:** 기존 Cancer-Myth·Well 평가 요소를 활용하여 FPQ 교정 개선, 정상 전제 보존, 일반 의료 QA 보존이 함께 성립하는 범위와 실패 조건을 보고한다.

세 항목은 기여의 성격을 나눈 것이다. 적용·정책 구성을 새로운 알고리즘 발명으로 포장하지 않는다. 지원하지 않는 결과는 실제 초록·서론에서 삭제하거나 제한한다.

## 3. Related Work — 네 묶음과 직접 선행연구

논문을 연도순으로 나열하지 않는다. **선행연구가 해결한 부분 → 우리에게 남는 평가 질문 → 대응 baseline** 순서로 쓴다.

| 묶음 | 선행연구에서 가져오는 것 | 이번 논문에서 남는 질문 | 실험 연결 |
|---|---|---|---|
| 의료 전제 교정과 정상 질문 보존 | Cancer-Myth의 FPQ/NFP·PCR/PCS·QA 평가; Well의 FPQ/TPQ 평가와 완화책 | 공개 모델에서 교정과 보존을 함께 달성하는가 | Table 2, Figure 1 |
| Prompting·CoT·추출 후 검증 | FP Identification, GEPA, extract-and-verify; CoT prompting과 monitoring | 명시적으로 전제를 검토하면 충분한가; 내부 신호가 추가로 유용한가 | Table 1·2, Table 3의 신호 비교 |
| 내부 readout과 행동 제어 | probe, 대조 방향, AO/NLA; 판별과 개입 효과의 구분 | 읽을 수 있는 정보가 있는가; 얻은 방향이 실제 교정을 유발하는가 | Table 1, C 후보의 개발 평가, 부록 |
| 조건부 개입과 직접 경쟁 방법 | CAST, Two Axes의 라우팅, Tripathi의 의료 gated steering | 같은 데이터·예산·개입 조건에서 어떤 부품이 기여하는가 | Table 2·3 |

**CAST:** 조건 벡터와 행동 벡터를 구분한 선행 방법이다. FPQ/TPQ로 적용한 CAST를 baseline으로 두고, 원 레시피에서 변경한 부분을 명시한다. CAST의 의료 전제 실패는 사전 사실이 아니다. [원문](https://arxiv.org/abs/2409.05907) · [구현](https://github.com/IBM/activation-steering)

**Two Axes:** answerability와 false-presupposition 구분 중 후자의 readout·라우팅 실험을 참고한다. 우리의 목표 라벨은 거짓 전제 유무이며 answerability 자체가 아니다. 논문의 일반 도메인 결과를 의료에서 재현했다고 미리 가정하지 않는다. 47%·61%는 전체 FPQ에서 NLI가 검증한 교정 비율이므로 gate 성능에 곱할 조건부 성공률로 쓰지 않는다. [원문 §6](https://arxiv.org/html/2607.08456v1#S6)

**Tripathi:** 의료 EHR·다중 턴 압력에서 내부 probe gate와 head steering, 정상 응답 평가를 이미 다룬다. 우리의 가장 직접적인 중복 후보 중 하나다. 공개 artifacts의 호환 모델 전이와 FPQ/TPQ로 재학습한 adaptation을 구분한다. 비교를 실행하지 않았다면 경험적 우위를 주장하지 않는다. [원문](https://arxiv.org/abs/2608.23666) · [공개 runtime 검토](11_tripathi_review.md)

**CoT 논의의 범위:** 충실성의 한계와 모니터링의 유용성을 함께 소개한다. CoT 연구 전체에 대한 반박을 논문의 동기로 삼지 않는다. [Turpin et al.](https://arxiv.org/abs/2305.04388) · [Monitoring Reasoning Models for Misbehavior](https://arxiv.org/abs/2503.11926)

**AO/NLA:** 현재 주 방법의 필수 부품이 아니다. probe보다 해석 가능한 설명을 얻는 부록 분석 후보로 둔다. 활성값 제거·섞기·텍스트만 입력 통제 없이 자연어 설명을 모델 내부의 진실로 읽지 않는다. AO/NLA 목적식 개선은 아직 이 논문의 확정 기여가 아니다.

## 4. Methodology — 재현 가능한 정책 명세

### 4.1 문제와 목표

입력 q는 환자 질문이다. y_FP는 전문가 검증 거짓 전제 유무이며, 평가·학습용으로만 제공된다. 최종 답변 r의 전제 교정(PCR/PCS), 정상 전제 오교정(TPQ), 공식 NFP 지표, 별도 의료 QA 정확도를 구분한다. NFP/TPQ가 같은 원 질문에서 나오면 두 배의 독립 표본으로 세지 않는다.

**전제 진위는 개입 이득의 대리 지표다.** FPQ라도 Plain이 이미 잘 고치면 추가 개입이 불필요할 수 있고, 개입이 답을 악화시킬 수도 있다. gate가 FPQ를 맞혔다고 교정 성공이나 인과적 이득이 보장되지는 않는다.

### 4.2 교정 대상 선택: gate

기본 내부 점수는 마지막 프롬프트 토큰의 고정 층 hidden state에 학습한 선형 probe를 적용해 얻는다.

```text
s(q) = sigmoid(w · h_l(q) + b)
g(q) = 1[s(q) ≥ τ]
```

층 l, 읽기 위치, 정규화, probe 정규화 강도, 문턱 τ는 내부 개발 분할에서 정한다. 원 질문에서 산출한 문항별 점수·mask를 저장한다. 전제 구간 readout을 비교할 때는 gold 전제 주석을 배포 입력으로 쓰지 않는다. 자동 추출을 사용하면 추출 비용·실패율을 포함하고, gold 구간 분석은 별도 진단으로 표시한다.

텍스트·CoT gate도 같은 목표 라벨을 예측한다. CoT monitor는 생성한 검토 텍스트를 읽어 gate를 정하고, 최종 답변은 별도 생성한다. 그 CoT를 최종 답변 입력에도 넣는다면 추가 개입이므로 별도 조건으로 구분한다. gate가 닫혀도 CoT 생성 비용은 발생한다.

### 4.3 선택된 질문에 적용할 교정 연산

최소 방법은 residual에 C 방향을 더한다. 실제 hook·토큰 구간·정규화는 구현과 함께 고정해 보고한다.

```text
선택한 층·생성 토큰에서: h' = h + g(q) · α · c
g(q) = 0이면 기준 생성 정책
```

C는 교정/비교정 응답 대조에서 얻는 후보 방향이다. 현재 후보는 자연 응답의 짝 없는 대조, 같은 질문의 시스템 프롬프트 짝 대조, 참조 답변을 teacher-forcing한 짝 대조다. 짝 대조는 질문 차이로 인한 교란을 줄이지만 내용·문체·질문과 응답의 상호작용을 정확히 제거하지 않는다. 학습에 쓰는 참조 답변·judge 라벨은 평가 fold에서 가져오지 않는다.

방향의 AUROC나 cos(A,C)만으로 교정 능력을 선택하지 않는다. 후보 방식·층·위치·α는 **내부 개발 문항에서 실제 응답을 생성하고**, 정상 질문 손실 제약 아래 교정 효과로 선택한다. cos(A,C)는 진단이며, 작다고 독립 회로가 증명되거나 크다고 분리가 실패하는 것은 아니다.

프롬프트 개입은 같은 gate가 선택한 질문에 전제 검사 지시를 추가하는 비교 조건이다. head steering·A∧C·직교화 C는 사전 명세한 확장/ablation이며 필수 성공 조건으로 추가하지 않는다.

### 4.4 개발 목표와 최종 정책 고정

```text
개발에서 최대화: FPQ 교정 성능
제약: NFP 점수 손실 ≤ ε_NFP
      TPQ 오교정률 증가 ≤ ε_TPQ
```

일차 지표, 동률 처리, 후보 예산, ε_NFP·ε_TPQ를 명시한다. QA는 [13](13_task_spec.md)의 규칙으로 정한 최종 시스템을 재조정 없이 적용하는 보존 평가다. 허용폭은 허용 가능한 손실에 근거해 정하며 표본 수만으로 정당화하지 않는다. 표본이 부족하면 CI가 결론을 내리지 못할 수 있다.

**보존은 경험적·통계적 평가다.** gate 오탐률만으로 보장되지 않는다. test 성능 차이의 신뢰구간과 사전 비열등성 기준으로 판정한다. 평균 차이가 작거나 유의하지 않다는 것만으로 보존을 선언하지 않는다.

## 5. Experimental Setup — 기존 표를 계승하는 방식

1. **데이터·중복:** Cancer-Myth 585 FPQ와 150 NFP/TPQ의 유효 ID·라벨 대응을 확인한다. 같은 myth, 의역, NFP–TPQ, 합성 참·거짓 쌍을 같은 그룹으로 묶는다. 합성 쌍은 원래 정상 질문 평가를 대체하지 않는다.
2. **평가 분할:** nested grouped 5-fold cross-fitting을 사용한다. probe·C·GEPA·CAST 등 모든 학습 부품은 outer train에서, 종류·층·문턱·강도 선택은 inner dev에서만 정한다. outer 평가 결과로 gate 우승자를 고른 뒤 같은 결과를 다시 확증 자료로 쓰지 않는다.
3. **모델·baseline:** 모델 revision·chat template·생성 설정·추가 데이터·탐색 예산을 보고한다. Table 2는 완성 방법을 공통 규칙 아래 조정한다. Table 3은 선택된 부품을 고정하고 하나만 바꾼다.
4. **판정:** PCR/PCS·공식 NFP는 Cancer-Myth 판정 절차, TPQ는 Well의 루브릭을 사용하되 judge가 다르면 adaptation으로 표시한다. TPQ 오교정률로 변환할 점수 경계는 결과를 보기 전에 고정한다. parse 실패를 정답으로 세지 않고, 미판정률·재시도 및 분모 처리 규칙을 공개한다.
5. **통계:** 주 지표·비열등성 허용폭·신뢰수준·다중 비교 처리를 고정한다. 같은 문항의 방법 간 차이를 짝지어 추정하고, myth·짝 문항의 의존성을 반영한다. 고정된 OOF 예측을 재표집한 CI는 학습/모델 선택의 전체 불확실성을 포함하지 않음을 명시한다. 방법별 CI의 겹침만으로 우열을 판정하지 않는다.
6. **QA:** MedQA·PubMedQA·Medbullets에 사전에 지정한 최종 정책을 적용한다. benchmark 이름으로 gate를 끄지 않는다. fold별 정책으로 낸 FPQ OOF 성능과 사전 지정한 최종 train/calibration 규칙으로 재적합한 QA 정책의 관계를 설명한다. 개별 QA의 CI와 발화율을 보고한다.
7. **원 표 재사용:** 데이터 수와 열이 같아도 cross-fitting·판정기·모델이 다르면 원본과 동일 실험이 아니다. Well의 실제 split ID를 확보하기 전에는 Table 8–10에 직접 비교 가능한 행을 붙였다고 쓰지 않는다. 출판 수치는 참고 행으로 분리하고 재실행 수치로 비교한다.

**현재 단계의 공개:** E1 중간 결과를 본 뒤 이 계획을 정교화했다. 따라서 모든 가설·선택이 어떤 데이터도 보기 전에 사전 등록됐다고 쓰지 않는다. 탐색에 사용한 데이터·설정과 이후 고정한 평가를 구분한다. 기존 탐색 데이터의 cross-fitting은 학습 누수를 줄이지만 이전 연구자 선택의 영향을 지우지 않는다. 완전히 새 held-out 검증을 추가할 경우 그 역할과 분할을 별도로 명시한다.

## 6. Experimental Results — 각 표에서 다른 질문에 답하기

### 6.1 Table 1: 무엇으로 교정 대상을 구분할 수 있는가? (Task 1, RH1)

**행:** 텍스트 기준선, 직접 전제 검사, extract-and-verify, CoT monitor, 내부 probe, 텍스트+내부 결합. **열:** AUROC와 CI, 개발에서 정한 낮은 FPR 운영점의 TPR/실제 FPR. 비용·지연은 별도 작은 표나 본문에 측정치로 보고한다.

**결과 문단에 쓸 순서:** 가장 중요한 짝 비교의 차이와 CI → 낮은 오탐 운영점 → 텍스트 결합의 이득 → 구간·문체·합성 쌍 통제 → 비용. TF-IDF는 텍스트 방법의 상한이 아니다. 같은 입력 범위와 정보 시점을 비교하고, 다르면 비용·가용 정보 차이를 명시한다.

**이 표만으로 못 하는 말:** 모델이 거짓임을 알고도 동조했다, 내부 probe가 실제 개입에서도 가장 좋다, C steering이 작동한다. 그중 실제 선택·개입 결과는 Table 3에서 확인한다. 모든 gate를 Task 2에 유지한다.

### 6.2 Table 2: 완성된 방법이 실제로 유용한가? (Task 2+3, RH2/RH3)

**행:** Plain, FP Identification, GEPA, 균형 전제 검토 CoT prompting, Unconditional C steering, CAST, Hidden gate + FP prompt, Hidden gate + C steering. Tripathi adaptation/전이는 구현·호환 범위에 맞춰 본 표 또는 부록에서 직접 비교한다. 실행하지 못한 범위는 제한으로 남긴다.

**열:** PCR, PCS, NFP, TPQ 오교정, MedQA, PubMedQA, Medbullets. 손실 방향을 통일하고 CI·허용폭을 명확히 표시한다. TPQ에서 참 전제를 부정하지 않았다는 것과 답변 내용 전체의 정확성은 다르다.

**결과 문단에 쓸 순서:** Plain 대비 FPQ 변화 → NFP/TPQ 비열등성 → 기존 방법 대비 같은 허용폭에서의 차이 → QA 보존 → 비용·실패 사례. 먼저 주 결과를 답하고 모델별 경향은 뒤에 설명한다. 다른 baseline이 같은 목표를 달성하면 그것도 보고한다.

**Figure 1:** 완성 시스템의 TPQ 보존(x)–PCR(y)을 그린다. 경계는 Plain TPQ 보존율 − ε_TPQ이다. 판별기만 있는 Table 1의 행은 좌표가 없으므로 찍지 않는다. Well Figure 1의 표현을 참고하되 PCR/오교정 비율을 쓰면 원문의 평균 점수 축과 다름을 표시한다.

### 6.3 Table 3: 어느 부품이 개선에 기여했는가? (Task 2, RH2/RH2b)

Table 2는 방법별 최종 정책을 비교한다. Table 3에서는 **우리 방법의 부품 하나만 바꾸는 세 비교**를 한다. 전체 방법 순위표나 불필요한 모든 조합을 반복하지 않는다.

| 비교 블록 | 고정하는 것 | 비교할 행 | 보고할 핵심 변화 |
|---|---|---|---|
| **A. 선택해서 개입할 가치** | 동일 C 방향·층·강도·토큰 스케줄 | 항상 / 무작위 / 내부 gate | PCR·PCS·정상 손실; 내부 vs 무작위로 선택의 가치, 내부 vs 항상으로 개입 빈도까지 포함한 정책 변화 |
| **B. 내부 신호로 선택할 가치** | 동일 C 개입, 사전에 정한 선택 예산 | 텍스트 / CoT / 내부 gate | 같은 개입 예산에서 PCR·PCS·정상 손실 차이 |
| **C. 활성값으로 개입할 가치** | 원 질문에서 저장한 동일한 문항별 gate mask | FP prompt / C steering | 동일 문항에 개입했을 때 교정 성능·품질과 정상 손실 차이 |

**실제 표에 들어갈 행 뼈대** (한 모델·한 고정 설정의 예, 모든 결과 칸은 비워 둠):

| ID | gate | 개입 | PCR [CI] | PCS [CI] | TPQ 오교정 [CI] | NFP [CI] | 개입률 |
|---|---|---|---|---|---|---|---|
| P | 없음 | Plain | | | | | 0 |
| U | 항상 | 고정 C | | | | | 100% |
| R | 무작위, 여러 seed | 고정 C | | | | | |
| T | 텍스트 | 고정 C | | | | | |
| Q | CoT monitor | 고정 C | | | | | |
| H | 내부 probe | 고정 C | | | | | |
| H-P | **H와 동일한 mask** | FP prompt | | | | | H와 동일 |

읽는 법: **U/R/H → 블록 A, T/Q/H → 블록 B, H/H-P → 블록 C**. 핵심 짝 비교의 Δ와 CI를 표의 패널 또는 바로 아래 문장으로 제시한다. QA 세 열은 Table 2에서 이미 평가하므로 여기서 반복할 필요가 없다. oracle과 A∧C/head는 추가 진단이며 표가 커지면 부록으로 둔다.

**개입 예산의 의미:** 개발에서 고정한 문턱은 test에서 정확히 같은 발화율을 보장하지 않는다. 문턱 정책의 실제 개입률을 보고하는 비교와, 사전에 정한 top-k 비율로 test 라벨 없이 예산을 맞추는 배치 진단을 구분한다. 후자는 단일 질문 배포 정책의 성능으로 부르지 않는다. FPQ/TPQ별 정답 라벨을 보고 선택 수를 조정하지 않는다. 같은 횟수에 개입해도 CoT 생성 비용까지 같은 것은 아니다. 정확한 실행 규칙은 [13](13_task_spec.md)에 둔다.

**Table 1과 Table 3의 차이:** Table 1은 거짓 전제를 구분하는 능력이다. Table 3-B는 그 선택으로 실제 교정 이득을 얻는지다. 탐지한 FPQ가 현재 C로 고칠 수 없는 문항이라면 두 결과는 다를 수 있다.

**Table 2와 중복을 다루는 규칙:**

- 모델·fold·부품·하이퍼파라미터·mask·생성·judge가 같으면 같은 결과를 재사용하고 원래 행을 참조한다. 별도 독립 실험처럼 세지 않는다.
- Table 2에서 각 방법을 따로 조정했다면, 이름이 같은 행도 Table 3의 고정 부품 조건과 다를 수 있다. config ID와 비교 목적을 표시한다.
- Table 2가 이미 세 통제 비교까지 충족하면 Table 3을 독립 표로 만들지 않고 구성 요소 분석 블록으로 합친다. 별도 표의 필요성은 추가로 답하는 질문과 지면에 달려 있다.

**캡션 초안:** "Controlled component comparisons for selective premise correction. We hold the correction operator fixed when comparing gates and reuse identical gate decisions when comparing interventions. Shared reference rows are reused from Table 2 only when configurations match."

### 6.4 결과가 다르게 나왔을 때의 서술

| 관찰 | 유지할 수 있는 주장 | 제한할 주장 |
|---|---|---|
| 내부 판별·선택과 C 개입이 모두 유리하고 보존 통과 | 내부 기반 선택적 교정의 효과와 각 부품의 추가 가치 | 임상 안전성·보편적 우위는 여전히 미검증 |
| 내부 판별은 좋지만 실제 개입 이득이 작음 | 판별 정보의 존재와 탐지–개입 간 차이 | 내부 gate의 최종 효용 |
| 텍스트/CoT gate가 동등하거나 더 좋음 | 선택 정책의 효과·비용 및 내부 readout의 한계 | 내부 신호가 필요하다는 주장 |
| 같은 gate에서 prompt와 C가 동등함 | gate의 효과 | steering의 성능상 추가 가치 |
| FPQ는 오르지만 정상/QA 비열등성이 미확인 | 관찰된 교정 개선과 불확실성 | 정상/QA 보존을 달성했다는 주장 |
| 기존 CAST/Tripathi adaptation이 동등하거나 더 좋음 | 기존 방법의 의료 전제 적용 결과와 구성 요소 분석 | 제안 방법의 우월성·새 알고리즘 필요성 |

CI가 넓어 비열등성을 확인하지 못한 경우와 허용폭을 넘는 손실이 확인된 경우를 구분한다. 결과에 따라 주제나 성공 기준을 소급 변경하지 않는다.

## 7. Discussion and Limitations — 해석의 범위

- **표상 해석:** probe의 예측 성공은 정의한 라벨이 읽힌다는 증거다. 모델의 지식·믿음·실제 사고 과정 전체를 증명하지 않는다. A×C 분포에 "알고도 동조" 같은 확정 원인 라벨을 붙이지 않는다.
- **설명 정확성:** PCR/PCS와 TPQ 보존은 모든 의학적 설명 단계의 정확성 또는 CoT faithfulness와 다르다. 설명 전체의 정확성을 주장하려면 별도의 내용 평가가 필요하다.
- **데이터와 통계:** 어려운 NFP의 작은 표본, 같은 원 질문의 중복, 문체·주제 shortcut, 합성 쌍의 생성 흔적, 탐색 후 설계 변경을 공개한다. NFP가 어려운 데이터라는 이유로 다른 논문의 정상 질문보다 반드시 어렵다고 정량 단정하지 않는다.
- **judge와 비용:** 자동 판정기의 오류·미판정, 모델과 judge 버전, readout 정보 시점, CoT 생성 및 hidden-state 접근 비용을 보고한다.
- **전이와 임상 적용:** 단일 턴 암 질문에서 나온 결과를 다른 진료 분야·EHR 다중 턴·실제 환자 안전으로 확장하지 않는다. QA 정확도 보존도 임상적 유효성 인증은 아니다.
- **독창성:** 구조의 출처와 적용 변경을 분명히 쓴다. 보존 제약 추가나 AO/NLA 사용만으로 새 방법론을 주장하지 않는다.

## 8. Conclusion — 검증된 결과만 세 문장으로

가설이 지지될 때의 틀:

> 본 연구는 의료 거짓 전제 교정에서 정상 질문의 손실을 제한하는 선택적 개입을 평가했다. [평가 범위]에서 [교정 개선과 보존 결과]를 확인했으며, 통제 비교를 통해 [선택 신호 및 개입 방식에 대해 실제로 지지된 결과]를 분리해 보였다. 이 결과는 [검증된 범위]에서 선택적 교정의 효용을 뒷받침하며, [미해결 전이·표본·판정 한계]는 남아 있다.

여기에 본문에 없던 숫자, 보편적인 "모델은 안다", "CoT는 실패한다", 최초의 조건부 steering, 임상 안전 보장을 추가하지 않는다. 일부 가설만 지지되면 그 부분만 남긴다.

## 9. 원고를 채우기 전에 남은 실제 작업

1. [16 §6–7](16_code_overview.md)의 분할·judge·TPQ 루브릭·캐시·resume scale 문제를 해결한다. 문서 갱신은 구현 완료가 아니다.
2. 일차 지표와 ε_NFP·ε_TPQ·QA별 허용폭, CI·다중 비교·미판정 처리, 최종 QA 정책 선택 규칙을 고정한다.
3. Table 1의 동일 입력 비교와 Table 3의 세 통제 비교를 config·mask ID 수준으로 명세한다. Table 2의 별도 최적화와 구분한다.
4. Table 1–3을 채운 뒤, Intro의 결과 예고·contribution과 Conclusion을 실제 지지 범위에 맞춰 작성한다. 추가 문헌 비교가 미실행이면 그대로 한계로 표시한다.

현재 E1 중간 수치는 [15](15_progress_summary.md)·[실험 기록](experiments/01-e1-prediagnostic.md)에 두고, 최종 결과 칸으로 옮기지 않는다.
